# URL del backend.
# En desarrollo local sin Docker: http://localhost:8000 (aunque el proxy de Vite lo maneja)
# En Docker: dejar vacío (Nginx hace el proxy)
# En producción (Vercel): URL completa del backend en Railway/Render
VITE_API_URL=
